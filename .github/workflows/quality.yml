name: Quality

on:
  pull_request:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8

      - name: Install base dependencies
        run: uv sync --locked --group dev

      - name: Run smoke suite
        run: bash scripts/run_smoke.sh

  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8

      - name: Install base dependencies
        run: uv sync --locked --group dev

      - name: Run tests
        run: uv run pytest -n auto --cov=cyoa --cov-report=term-missing --cov-report=xml --cov-report=json -q

      - name: Generate coverage badge
        run: |
          mkdir -p .github/badges
          uv run genbadge coverage -i coverage.xml -o .github/badges/coverage.svg

      - name: Enforce package coverage floors
        run: uv run python scripts/check_coverage.py

      - name: Upload coverage artifacts
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: |
            coverage.json
            coverage.xml
            .github/badges/coverage.svg

      - name: Commit coverage badge
        if: github.event_name == 'push'
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "ci: update coverage badge"
          file_pattern: .github/badges/coverage.svg


  ruff:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8

      - name: Install base dependencies
        run: uv sync --locked --group dev

      - name: Run Ruff
        run: uv run ruff check .

  mypy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8

      - name: Install base dependencies
        run: uv sync --locked --group dev

      - name: Run mypy
        run: uv run mypy cyoa

  optional-deps:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8

      - name: Install optional test dependencies
        run: uv sync --locked --group dev

      - name: Install numpy for interruption tests
        run: uv pip install "numpy>=2.1"

      - name: Run optional dependency coverage
        run: >
          uv run pytest -n auto -q
          tests/test_sqlite_db.py
          tests/test_speculative_interruption.py
          --cov=cyoa.db.sqlite_db
          --cov=cyoa.llm.providers
          --cov-report=term-missing
          --cov-report=xml:coverage-optional.xml
          --cov-report=json:coverage-optional.json

      - name: Upload optional coverage artifacts
        uses: actions/upload-artifact@v4
        with:
          name: optional-coverage-report
          path: |
            coverage-optional.json
            coverage-optional.xml

  runtime-extras:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8

      - name: Install runtime extras
        run: uv sync --locked --group dev --extra memory --extra observability

      - name: Run runtime extras coverage
        run: >
          uv run pytest -n auto -q
          tests/test_runtime_extras.py
          tests/test_observability.py
          tests/test_story.py
          -k "setup_observability or NarrativeMemory or NPCMemory"
